// Code generated by moq; DO NOT EDIT.
// github.com/matryer/moq

package mock

import (
	"context"
	"sync"

	"github.com/oxynote/oxynote/server/core/internal/assistant/edit"
	"github.com/oxynote/oxynote/server/core/internal/assistant/tools"
	"github.com/rs/xid"
)

// Ensure, that EditApplier does implement tools.EditApplier.
// If this is not the case, regenerate this file with moq.
var _ tools.EditApplier = &EditApplier{}

// EditApplier is a mock implementation of tools.EditApplier.
//
//	func TestSomethingThatUsesEditApplier(t *testing.T) {
//
//		// make and configure a mocked tools.EditApplier
//		mockedEditApplier := &EditApplier{
//			ApplyFunc: func(ctx context.Context, documentID xid.ID, branchID xid.ID, ops []edit.Operation, userID string, system bool) (edit.Result, error) {
//				panic("mock out the Apply method")
//			},
//			FlushFunc: func(ctx context.Context, documentID xid.ID, branchID xid.ID) error {
//				panic("mock out the Flush method")
//			},
//		}
//
//		// use mockedEditApplier in code that requires tools.EditApplier
//		// and then make assertions.
//
//	}
type EditApplier struct {
	// ApplyFunc mocks the Apply method.
	ApplyFunc func(ctx context.Context, documentID xid.ID, branchID xid.ID, ops []edit.Operation, userID string, system bool) (edit.Result, error)

	// FlushFunc mocks the Flush method.
	FlushFunc func(ctx context.Context, documentID xid.ID, branchID xid.ID) error

	// calls tracks calls to the methods.
	calls struct {
		// Apply holds details about calls to the Apply method.
		Apply []struct {
			// Ctx is the ctx argument value.
			Ctx context.Context
			// DocumentID is the documentID argument value.
			DocumentID xid.ID
			// BranchID is the branchID argument value.
			BranchID xid.ID
			// Ops is the ops argument value.
			Ops []edit.Operation
			// UserID is the userID argument value.
			UserID string
			// System is the system argument value.
			System bool
		}
		// Flush holds details about calls to the Flush method.
		Flush []struct {
			// Ctx is the ctx argument value.
			Ctx context.Context
			// DocumentID is the documentID argument value.
			DocumentID xid.ID
			// BranchID is the branchID argument value.
			BranchID xid.ID
		}
	}
	lockApply sync.RWMutex
	lockFlush sync.RWMutex
}

// Apply calls ApplyFunc.
func (mock *EditApplier) Apply(ctx context.Context, documentID xid.ID, branchID xid.ID, ops []edit.Operation, userID string, system bool) (edit.Result, error) {
	callInfo := struct {
		Ctx        context.Context
		DocumentID xid.ID
		BranchID   xid.ID
		Ops        []edit.Operation
		UserID     string
		System     bool
	}{
		Ctx:        ctx,
		DocumentID: documentID,
		BranchID:   branchID,
		Ops:        ops,
		UserID:     userID,
		System:     system,
	}
	mock.lockApply.Lock()
	mock.calls.Apply = append(mock.calls.Apply, callInfo)
	mock.lockApply.Unlock()
	if mock.ApplyFunc == nil {
		var (
			resultOut edit.Result
			errOut    error
		)
		return resultOut, errOut
	}
	return mock.ApplyFunc(ctx, documentID, branchID, ops, userID, system)
}

// ApplyCalls gets all the calls that were made to Apply.
// Check the length with:
//
//	len(mockedEditApplier.ApplyCalls())
func (mock *EditApplier) ApplyCalls() []struct {
	Ctx        context.Context
	DocumentID xid.ID
	BranchID   xid.ID
	Ops        []edit.Operation
	UserID     string
	System     bool
} {
	var calls []struct {
		Ctx        context.Context
		DocumentID xid.ID
		BranchID   xid.ID
		Ops        []edit.Operation
		UserID     string
		System     bool
	}
	mock.lockApply.RLock()
	calls = mock.calls.Apply
	mock.lockApply.RUnlock()
	return calls
}

// Flush calls FlushFunc.
func (mock *EditApplier) Flush(ctx context.Context, documentID xid.ID, branchID xid.ID) error {
	callInfo := struct {
		Ctx        context.Context
		DocumentID xid.ID
		BranchID   xid.ID
	}{
		Ctx:        ctx,
		DocumentID: documentID,
		BranchID:   branchID,
	}
	mock.lockFlush.Lock()
	mock.calls.Flush = append(mock.calls.Flush, callInfo)
	mock.lockFlush.Unlock()
	if mock.FlushFunc == nil {
		var (
			errOut error
		)
		return errOut
	}
	return mock.FlushFunc(ctx, documentID, branchID)
}

// FlushCalls gets all the calls that were made to Flush.
// Check the length with:
//
//	len(mockedEditApplier.FlushCalls())
func (mock *EditApplier) FlushCalls() []struct {
	Ctx        context.Context
	DocumentID xid.ID
	BranchID   xid.ID
} {
	var calls []struct {
		Ctx        context.Context
		DocumentID xid.ID
		BranchID   xid.ID
	}
	mock.lockFlush.RLock()
	calls = mock.calls.Flush
	mock.lockFlush.RUnlock()
	return calls
}
